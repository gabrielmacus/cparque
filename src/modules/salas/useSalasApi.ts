/*import useApi from "../common/hooks/useApi"

export interface Sala
{
    nombre:string
}

export default () => {
    
    return useApi<Sala>({
        objProps:["nombre"],
        sheetName:"Salas",
        generateId:(data)=>`${data.nombre}`
    });
}*/

export default () => {};