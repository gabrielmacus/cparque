/*import useApi from "../common/hooks/useApi"

export interface TipoIntervencion  
{
    nombre:string
}

export default () => {
    
    return useApi<TipoIntervencion>({
        objProps:["nombre"],
        sheetName:"Tipos de intervención",
        generateId:(data)=>`${data.nombre}`
    });
}*/

export default () => {}